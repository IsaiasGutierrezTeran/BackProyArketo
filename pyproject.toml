# Estandar de codificacion del backend (PEP 8).
# Formatear con:  python -m black .
# Ordenar imports: python -m isort .
[tool.black]
line-length = 79
extend-exclude = "/migrations/"

[tool.isort]
profile = "black"
line_length = 79
skip_glob = ["*/migrations/*"]
