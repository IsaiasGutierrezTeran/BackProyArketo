from django.apps import AppConfig


class Sketch2DConfig(AppConfig):
    """Sprint 3 — HU-18: generar boceto/plano 2D a partir de un prompt (IA)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sketch_2d"
    verbose_name = "Boceto 2D por prompt"
