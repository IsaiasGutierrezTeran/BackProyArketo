from django.apps import AppConfig


class BudgetConfig(AppConfig):
    """Sprint 3 — CU11 materiales · CU12 presupuesto · CU13 revisión Ingeniero."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "budget"
    verbose_name = "Presupuesto"
