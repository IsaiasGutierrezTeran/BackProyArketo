from django.apps import AppConfig


class PlansConfig(AppConfig):
    """Sprint 2 — CU4 subir y validar planos (PDF/JPG/PNG/CSV)."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "plans"
    verbose_name = "Planos 2D"
