from django.apps import AppConfig


class ProjectsConfig(AppConfig):
    """Sprint 1 — Project (entidad raíz) + dashboard. Sprint 4 — colaboración."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "projects"
    verbose_name = "Proyectos"
